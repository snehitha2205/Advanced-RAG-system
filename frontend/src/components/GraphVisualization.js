import React from 'react';
import KnowledgeExplorer from './knowledge/KnowledgeExplorer';

const GraphVisualization = ({ darkMode }) => {
  return <KnowledgeExplorer darkMode={darkMode} />;
};

export default GraphVisualization;
